[build]
  command = "npm install && npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "zisi"

# Редирект для чата
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/index/:splat"
  status = 200